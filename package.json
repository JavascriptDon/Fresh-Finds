{
  "name": "social-finds",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build-extension": "npm run build && xcopy manifest.json dist/"
  },
  "devDependencies": {
    "vite": "^4.3.9"
  }
}
